spring:
  application:
    name: one
server:
  port: 8081
eureka:
  client:
    serviceUrl:
#      defaultZone: http://discovery:8761/eureka
      defaultZone: http://localhost:8761/eureka
  instance:
    preferIpAddress: true